range = prompt("Enter first number of range and a second number for the end of the range")
funtion range (start, end) {
  var ans = []; 
  for (let i = start; i<= end; i++)  
    ans.push(i);
    }
    return ans; 
    }
    